<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ywh.demo.mapper.YinliSubjectConfigMappereee">

<update id="update">
        UPDATE yinli_subject_config set code=#{code} WHERE id=#{id}
</update>


<select id="getlist" resultType="com.ywh.demo.model.YinliSubjectConfig">
        select * from yinli_subject_config
        WHERE id = #{id}
</select>
</mapper>